{{#each this.shapes as |s|}}
  <details>
    <summary><Sigil @ref={{s.ref}} @size={{64}}/></summary>
    {{#each s.forms as |f|}}
      <Sigil @ref={{f.ref}} @size={{64}}/>
    {{/each}}
  </details>
{{/each}}
